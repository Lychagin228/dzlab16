# Работа с массивами в C

## 📋 Задания к лаб. 11

1. Заполнение массива с консоли
2. Преобразование массива
3. Печать массива
4. Количество положительных элементов массива, расположенных до максимального элемента.

## 📋 Задания к лаб. 14

1. Заполнение массива значениями из функции Func(x)
2. Печать массива
3. Вычисление суммы элементов массива
4. Поиск в массиве значения равного a
5. Поиск максимального значения, среди элементов порядковый номер которых лежит в диапазоне [K; L].

## 📋 Задания к лаб. 16

1. Напишите программу, которая работает с динамическим массивом
2. Перепишите программу так, чтобы массив хранил значения типа double, заполните его случайными числами от -1 до 1.
2.1. Удаление из массива каждого k-го элемента.
2.2. Вставка после каждого k-го элемента.
3. Сгенерировать три массива со случайными вещественными числами а0,
а1, а2, ..., an, b0, b1, b2 ..., bm, с0, с1,с2, ..., сl, количество элементов случайно в
интервале от 10 до 50. Реализовать функцию, которая возвращает новый
массив d0, d1, d2, ..., dh в котором разместить сначала все элементы
массива а0,а1 , а2, ...аn, затем элементы массива b0,b1, b2, ..., bm в обратном
порядке и в конце накопленную сумму элементов массива с0,с1 , с2, ..., сl.. Все 4 массива вывести на экран.

## 📁 Структура проекта

├── .gitattributes

├── .gitignore

├── DZ.c

├── arrays.h

├── arrays.c

├── dzlab16.slnx

├── dzlab16.vcxproj

├── dzlab16.vcxproj.filters

└── README.md

## 🔧 Реализованные функции

1. double* fill_array_random - Заполнение массива случайными числами
Параметры: array - указатель на массив, size - размер массива
Возвращает: указатель на заполненный массив
Примечание: Заполняет массив случайными числами от -100 до 100

2. int find_max_element - Поиск максимального элемента в массиве
Параметры: array - указатель на массив, size - размер, max_value - указатель на переменную для максимального значения
Возвращает: индекс максимального элемента
Примечание: Возвращает первый максимальный элемент, если их несколько

3. int count_positive_before_index - Подсчет положительных элементов до указанного индекса
Параметры: array - указатель на массив, size - размер, index - индекс до которого считать
Возвращает: количество положительных элементов
Примечание: Индекс не включается в подсчет

4. int print_array_double - Вывод массива типа double
Параметры: array - указатель на массив, size - размер
Возвращает: 0 при успехе, -1 при ошибке
Примечание: Форматированный вывод по 5 элементов в строку

5. int delete_each_kth_element - Удаление каждого k-го элемента
Параметры: array - указатель на массив, size - размер, k - шаг удаления
Возвращает: новый размер массива
Примечание: Изменяет исходный массив без перевыделения памяти

6. double* insert_after_each_kth - Вставка значения после каждого k-го элемента
Параметры: array - указатель на массив, size - указатель на размер, k - шаг, value - значение для вставки
Возвращает: указатель на измененный массив
Примечание: Перевыделяет память при необходимости

7. double* create_random_array - Создание массива случайного размера со случайными значениями
Параметры: size - указатель на переменную для размера
Возвращает: указатель на созданный массив
Примечание: Размер 10-50, значения -50 до 50

8. int print_array_info - Вывод информации о массиве с именем
Параметры: array - указатель на массив, size - размер, name - имя массива
Возвращает: 0 при успехе, -1 при ошибке
Примечание: Выводит имя массива перед элементами

9. double* combine_arrays - Объединение трех массивов по правилу
Параметры: a,b,c - указатели на массивы, n,m,l - их размеры, result_size - указатель на результирующий размер
Возвращает: указатель на новый массив
Примечание: Правило: A + B в обратном порядке + сумма элементов C

10. double calculate_cumulative_sum - Вычисление суммы всех элементов массива
Параметры: array - указатель на массив, size - размер
Возвращает: сумму элементов
Примечание: Простое суммирование всех значений

11. int* create_int_array - Создание целочисленного массива
Параметры: size - размер массива
Возвращает: указатель на созданный массив
Примечание: Выделяет память для массива int

12. int fill_int_array - Заполнение целочисленного массива с клавиатуры
Параметры: array - указатель на массив, size - размер
Возвращает: 0 при успехе, -1 при ошибке
Примечание: Запрашивает ввод у пользователя

13. int find_max_in_range - Поиск максимума в диапазоне [K, L]
Параметры: array - указатель на массив, size - размер, K - начальный индекс, L - конечный индекс
Возвращает: максимальное значение в диапазоне
Примечание: Границы включительно, K ≤ L

14. int print_int_array - Вывод целочисленного массива
Параметры: array - указатель на массив, size - размер
Возвращает: 0 при успехе, -1 при ошибке
Примечание: Выводит в формате [элемент, элемент, ...]

15. int execute_main_task - Выполнение основного задания
Параметры: нет
Возвращает: 0 при успешном выполнении
Примечание: Полный цикл работы с массивом (заполнение, поиск максимума, удаление, вставка)

16. int execute_three_arrays_task - Выполнение задания с тремя массивами
Параметры: нет
Возвращает: 0 при успешном выполнении
Примечание: Создание, вывод и объединение трех массивов

17. int execute_max_in_range_task - Выполнение задания поиска максимума в диапазоне
Параметры: нет
Возвращает: 0 при успешном выполнении
Примечание: Работа с целочисленным массивом, ввод диапазона


Подготовил Лычагин Антон. бИЦ-252.
